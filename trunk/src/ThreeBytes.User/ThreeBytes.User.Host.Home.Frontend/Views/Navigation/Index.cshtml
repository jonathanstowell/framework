@model ThreeBytes.User.Host.Home.Frontend.Models.NavigationViewModel

@Html.RenderNavigation(Model.Navigation)

@{ 
  using (Html.BeginScriptContext())
  {
    Html.AddScriptBlock(@"
        jQuery(document).ready(function() {
            $('#topbar').dropdown();
        });");
  }
}